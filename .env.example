# Copier en .env pour personnaliser (facultatif).
PORT=3000
# true = navigateur invisible (defaut, recommande) ; false = visible (debug).
HEADLESS=true
# Delai max (ms) d'attente des elements de page.
NAV_TIMEOUT=45000
